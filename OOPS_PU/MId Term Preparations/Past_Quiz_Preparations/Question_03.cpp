/*
Complete code to replace existing color of every pixel randomly with some other color. (Do not write image)
ifstream in("abc.pgm",ios::binary);
...
in >> width >> height >> maxval;
in.read((char*) pixels, width * height);
*/

